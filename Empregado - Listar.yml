info:
  name: Empregado - Listar Todos
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8080/empregado
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
