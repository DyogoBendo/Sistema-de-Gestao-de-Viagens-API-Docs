info:
  name: Motivo - Listar
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8080/motivo
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
