info:
  name: Área - Editar
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/area/1
  body:
    type: json
    data: |-
      {
        "nome": "GE.DT Atualizada"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
