info:
  name: Cargo - Editar
  type: http
  seq: 17

http:
  method: PUT
  url: http://localhost:8080/cargo/3
  body:
    type: json
    data: |-
      {
        "nome": "Colaborador Sênior"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
