info:
  name: Cadastrar empregado
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/empregado
  body:
    type: json
    data: |-
      {
        "matricula": "ABCD",
        "nome": "Joao Pedro",
        "area": "GEDT"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
