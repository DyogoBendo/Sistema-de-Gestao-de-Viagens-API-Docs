info:
  name: Viagem - Listar por Status (Gestor)
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:8080/viagem/status/Solicitada
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
